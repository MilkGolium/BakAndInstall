#ifndef TEST_H
#define TEST_H

// runTests: run all API tests against an in-memory SQLite database.
// Prints pass/fail results for each test case.
// Returns 0 if all pass, 1 if any fail.
int runTests(void);

#endif // TEST_H
